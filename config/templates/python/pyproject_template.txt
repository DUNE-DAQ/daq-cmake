[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
name = "{PACKAGE_NAME}"
requires-python = ">=3.10"
readme = "docs/README.md"
dynamic = ["version"]
description = "{DESCRIPTION}"

dependencies = [
    {DEPENDENCIES}
]


[tool.hatch.version]
path = "version.py"

[tool.hatch.packages.find]
where = ["src"]

[project.optional-dependencies]
dev = ["ruff", "pre-commit", "pytest"]

{APPLICATIONS}


[tool.ruff]
exclude = [
    {COMPILED_SCHEMA_LOCATIONS}
]

[tool.ruff.lint]
select = [
    "D",   # pydocstyle
    "E",   # pycodestyle
    "F",   # Pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "RUF", # ruff-specific rules
]
pydocstyle.convention = "google"